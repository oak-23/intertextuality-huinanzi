/*
 * SettingsDropdown is implemented in `TopBar/ModeSwitch.tsx` as the trigger +
 * Popover combination. This file re-exports it under the spec'd name so
 * consumers can import either path.
 */
export { ModeSwitch as SettingsDropdown } from '../TopBar/ModeSwitch';
